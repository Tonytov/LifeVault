// 🗄️ LifeVault Database Schema
// Комплексная схема для отслеживания привычек и продолжительности жизни

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 👤 Основная модель пользователя
model User {
  id                String             @id @default(uuid())
  phoneNumber       String             @unique
  isPhoneVerified   Boolean            @default(false)
  verificationCode  String?
  codeExpiresAt     DateTime?
  
  // JWT токены
  refreshTokens     RefreshToken[]
  
  // Профиль пользователя
  profile          UserProfile?
  
  // Привычки
  habits           Habit[]
  
  // Система вызовов и достижений
  challengeProgress UserChallengeProgress[]
  achievements     UserAchievement[]
  
  // Банк времени жизни
  timeBankAccount  TimeBankAccount?
  timeBankTransactions TimeBankTransaction[]
  
  // Системные поля
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  lastLoginAt      DateTime?
  
  @@map("users")
}

// 🔄 Refresh токены для безопасной аутентификации
model RefreshToken {
  id        String   @id @default(uuid())
  token     String   @unique
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
  
  @@map("refresh_tokens")
}

// 📝 Детальный профиль пользователя
model UserProfile {
  id              String    @id @default(uuid())
  userId          String    @unique
  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  // Основная информация
  firstName       String?
  lastName        String?
  dateOfBirth     DateTime
  gender          Gender
  
  // Физические параметры
  height          Int       // в сантиметрах
  weight          Int       // в килограммах
  region          String    // ISO код страны/региона
  
  // Настройки приватности
  isPublicProfile Boolean   @default(false)
  
  // Временные метки
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  
  @@map("user_profiles")
}

// 🚭 Привычки пользователя
model Habit {
  id              String      @id @default(uuid())
  userId          String
  user            User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  type            HabitType
  isActive        Boolean     @default(true)
  
  // Параметры привычки
  dailyUsage      Int?        // количество в день (сигарет, стаканов и т.д.)
  costPerUnit     Float?      // стоимость за единицу
  unitsPerPack    Int?        // единиц в пачке (для курения)
  
  // Даты
  startDate       DateTime?   // когда начал привычку
  quitDate        DateTime?   // когда бросил
  targetQuitDate  DateTime?   // планируемая дата отказа
  
  // Дополнительная информация
  additionalInfo  String?
  
  // История изменений
  habitLogs       HabitLog[]
  
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
  
  @@map("habits")
}

// 📊 Лог изменений привычек
model HabitLog {
  id        String    @id @default(uuid())
  habitId   String
  habit     Habit     @relation(fields: [habitId], references: [id], onDelete: Cascade)
  
  // Что изменилось
  action    HabitLogAction
  oldValue  Json?     // старые значения (JSON)
  newValue  Json?     // новые значения (JSON)
  notes     String?
  
  createdAt DateTime  @default(now())
  
  @@map("habit_logs")
}

// 🎯 Система дневных вызовов
model Challenge {
  id              String                  @id @default(uuid())
  
  title           String
  description     String
  category        ChallengeCategory
  difficulty      ChallengeDifficulty
  duration        ChallengeDuration
  
  // Система наград
  rewardTimeBank  Int                     // дни жизни за выполнение
  
  // Условия активации
  isActive        Boolean                 @default(true)
  requiresHabit   HabitType?             // требует определенную привычку
  
  // Связи
  userProgress    UserChallengeProgress[]
  
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt
  
  @@map("challenges")
}

// 📈 Прогресс пользователя в вызовах
model UserChallengeProgress {
  id              String            @id @default(uuid())
  userId          String
  challengeId     String
  
  user            User              @relation(fields: [userId], references: [id], onDelete: Cascade)
  challenge       Challenge         @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  
  status          ChallengeStatus   @default(IN_PROGRESS)
  startDate       DateTime
  endDate         DateTime?
  
  // Метрики прогресса
  currentStreak   Int               @default(0)
  maxStreak       Int               @default(0)
  completedDays   Int               @default(0)
  totalDays       Int
  progress        Float             @default(0) // от 0 до 1
  
  // Награды
  rewardClaimed   Boolean           @default(false)
  rewardClaimedAt DateTime?
  
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  
  @@unique([userId, challengeId, startDate])
  @@map("user_challenge_progress")
}

// 🏆 Система достижений
model Achievement {
  id              String              @id @default(uuid())
  
  name            String
  description     String
  category        AchievementCategory
  iconEmoji       String              // эмодзи иконка
  
  // Условия получения
  conditions      Json                // JSON с условиями
  rewardTimeBank  Int                 // награда в днях жизни
  
  isActive        Boolean             @default(true)
  rarity          AchievementRarity   @default(COMMON)
  
  // Связи
  userAchievements UserAchievement[]
  
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt
  
  @@map("achievements")
}

// 🎖️ Достижения пользователей
model UserAchievement {
  id              String      @id @default(uuid())
  userId          String
  achievementId   String
  
  user            User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  achievement     Achievement @relation(fields: [achievementId], references: [id], onDelete: Cascade)
  
  unlockedAt      DateTime    @default(now())
  rewardClaimed   Boolean     @default(false)
  
  @@unique([userId, achievementId])
  @@map("user_achievements")
}

// 🏦 Банк времени жизни - кошелек пользователя
model TimeBankAccount {
  id              String                @id @default(uuid())
  userId          String                @unique
  user            User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  // Баланс в днях жизни
  totalDaysEarned Int                   @default(0)
  totalDaysSpent  Int                   @default(0)
  currentBalance  Int                   @default(0)
  
  // История транзакций
  transactions    TimeBankTransaction[]
  
  createdAt       DateTime              @default(now())
  updatedAt       DateTime              @updatedAt
  
  @@map("timebank_accounts")
}

// 💳 Транзакции банка времени
model TimeBankTransaction {
  id              String              @id @default(uuid())
  userId          String
  accountId       String
  
  user            User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  account         TimeBankAccount     @relation(fields: [accountId], references: [id], onDelete: Cascade)
  
  type            TransactionType
  amount          Int                 // в днях жизни (может быть отрицательным для трат)
  description     String
  
  // Связанные сущности
  relatedEntity   String?             // тип сущности (challenge, achievement, purchase)
  relatedId       String?             // ID связанной сущности
  
  createdAt       DateTime            @default(now())
  
  @@map("timebank_transactions")
}

// 🛒 Магазин улучшений (опционально)
model StoreItem {
  id              String      @id @default(uuid())
  
  name            String
  description     String
  category        StoreCategory
  price           Int         // цена в днях жизни
  iconEmoji       String
  
  // Эффекты покупки
  effects         Json        // JSON с эффектами
  
  isActive        Boolean     @default(true)
  isLimited       Boolean     @default(false)
  maxPurchases    Int?        // максимум покупок на пользователя
  
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
  
  @@map("store_items")
}

// 🔍 Система аналитики и статистики
model UserStats {
  id              String    @id @default(uuid())
  userId          String    @unique
  
  // Кэшированная статистика для быстрого доступа
  totalChallengesStarted    Int @default(0)
  totalChallengesCompleted  Int @default(0)
  longestStreak            Int @default(0)
  currentStreak            Int @default(0)
  
  // Статистика привычек
  daysSmokeFree            Int @default(0)
  cigarettesNotSmoked      Int @default(0)
  moneySevedTotal          Float @default(0)
  
  // Расчеты продолжительности жизни
  baseLifeExpectancy       Float
  currentLifeExpectancy    Float
  lifeExtensionDays        Int @default(0)
  
  // Временные метки
  lastCalculatedAt         DateTime @default(now())
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
}

// 📱 Push уведомления
model PushNotification {
  id              String              @id @default(uuid())
  userId          String
  
  title           String
  body            String
  type            NotificationType
  data            Json?               // дополнительные данные
  
  sentAt          DateTime?
  isRead          Boolean             @default(false)
  readAt          DateTime?
  
  createdAt       DateTime            @default(now())
  
  @@map("push_notifications")
}

// ==================== ENUMS ====================

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum HabitType {
  SMOKING
  ALCOHOL  
  SUGAR
}

enum HabitLogAction {
  CREATED
  UPDATED
  QUIT
  RESUMED
  DELETED
}

enum ChallengeCategory {
  QUIT_HABITS
  HEALTH_IMPROVEMENT
  MENTAL_WELLNESS
  FITNESS
  NUTRITION
}

enum ChallengeDifficulty {
  BEGINNER
  INTERMEDIATE
  ADVANCED
  EXPERT
}

enum ChallengeDuration {
  ONE_DAY
  THREE_DAYS
  ONE_WEEK
  TWO_WEEKS
  ONE_MONTH
  THREE_MONTHS
  SIX_MONTHS
  ONE_YEAR
}

enum ChallengeStatus {
  IN_PROGRESS
  COMPLETED
  FAILED
  PAUSED
}

enum AchievementCategory {
  HABITS
  CHALLENGES
  TIME_SAVED
  SOCIAL
  MILESTONES
}

enum AchievementRarity {
  COMMON
  RARE
  EPIC
  LEGENDARY
}

enum TransactionType {
  EARNED
  SPENT
  BONUS
  REFUND
}

enum StoreCategory {
  BOOSTERS
  COSMETICS
  PREMIUM_FEATURES
  HEALTH_INSIGHTS
}

enum NotificationType {
  CHALLENGE_REMINDER
  HABIT_MILESTONE
  ACHIEVEMENT_UNLOCKED
  DAILY_MOTIVATION
  STREAK_WARNING
}